import { OpenOnClickDirective } from './open-on-click.directive';

describe('OpenOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
